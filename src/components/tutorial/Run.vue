<template>
    <h1>Run</h1>
    <p>Click <code>Save Changes</code> to save the current file.</p>
    <p>Click <code>Run</code> to execute the current file.</p>
    <p>Click <code>Stop</code> to shut down the running code.</p>
    <p>Stopping the code pauses the game, but doesn't terminate the bots you created.</p>
    <p>If a fatal (uncaught) error occurs, the code will stop automatically.</p>
    <p>For changes to be reflected in the running code, files must be saved.</p>
    <p>On desktop, you can also hit <code>Ctrl+S</code> to save.</p>
</template>
