<template>
    <h1>Tools</h1>
    <p>The highlighted area contains some farm manipulation tools.</p>
    <p>
        Select the <code>Inspector</code>, and click on a bot to inspect it.
        <br>
        The inspector also shows whether the code is running, and will show the last error.
        <br>
        Hover over an animal to see its energy.
    </p>
    <p>The <code>Tile Placer</code> lets you modify the material of a tile.</p>
    <p>The <code>Fence Placer</code> lets you place fences (shocking).</p>
    <p>The <code>Clear Tile</code> tool removes the object on the tile (e.g. fence, crops).</p>
</template>
